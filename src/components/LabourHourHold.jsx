import React from 'react';
import Card from '../utils/Card';

const LabourHourHold = () => {
    return (
        <Card
            topic="Labour Hour Hold"
            description="Leverages OAuth 2.0 and OpenID Connect for secure user authentication and delegated access."
        />
    );
};

export default LabourHourHold;