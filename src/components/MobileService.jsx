import React from 'react';
import Card from "../utils/Card";

const MobileService = () => {
    return (
        <Card
            topic="Mobile Service"
            description="Includes firewalls, intrusion detection systems, and encryption protocols to safeguard data."
        />
    );
};

export default MobileService;